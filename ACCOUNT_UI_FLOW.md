# 사용자 계정 페이지 UI Flow

## 📋 개요

`/account` 페이지는 사용자의 계정 정보, OAuth 연결 상태, 활동 통계, 세션 정보를 표시하는 페이지입니다.

---

## 🔄 전체 Flow 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                    사용자 접근 경로                              │
├─────────────────────────────────────────────────────────────────┤
│  1. Header에서 프로필 이미지/이름 클릭                          │
│  2. 직접 URL 입력: /account                                      │
│  3. 다른 페이지에서 링크 클릭                                    │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│          Server Component: app/(main)/account/page.tsx            │
├─────────────────────────────────────────────────────────────────┤
│  • getServerSession(authOptions) 호출                            │
│  • 세션 확인                                                    │
└─────────────────────────────────────────────────────────────────┘
                            ↓
                    ┌───────────────┐
                    │ 세션 존재?     │
                    └───────────────┘
                    ↙              ↘
            [YES]                    [NO]
            ↓                        ↓
┌──────────────────────┐   ┌──────────────────────────────┐
│ AccountInfo 컴포넌트  │   │ redirect('/login?callbackUrl │
│ 렌더링               │   │ =/account')                  │
└──────────────────────┘   └──────────────────────────────┘
            ↓                        ↓
┌─────────────────────────────────────────────────────────────────┐
│     Client Component: components/account/AccountInfo.tsx         │
├─────────────────────────────────────────────────────────────────┤
│  1. useSession() 훅으로 세션 상태 확인                           │
│  2. status === 'authenticated' 확인                             │
│  3. fetchAccountInfo() 호출                                      │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│              API 호출: GET /api/users/me                          │
├─────────────────────────────────────────────────────────────────┤
│  • getServerSession()으로 인증 확인                              │
│  • session.user.id 추출                                          │
│  • Prisma로 사용자 데이터 조회:                                  │
│    - User 정보 (기본 정보)                                       │
│    - Accounts (OAuth 연결 정보)                                  │
│    - Sessions (최근 5개 세션)                                    │
│    - _count (리뷰, 즐겨찾기 수)                                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                    데이터 표시                                    │
├─────────────────────────────────────────────────────────────────┤
│  • 로딩 상태: 스피너 표시                                        │
│  • 에러 상태: 에러 메시지 + Toast                                │
│  • 성공 상태: 5개 카드 섹션 표시                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📍 접근 경로

### 1. Header에서 접근
```tsx
// components/layout/Header.tsx
<Link href="/account">
  {session.user?.image ? (
    <img src={session.user.image} ... />
  ) : (
    <User icon />
  )}
  <span>{session.user?.name || session.user?.email}</span>
</Link>
```

**Flow:**
- 사용자가 로그인 상태
- Header의 프로필 이미지 또는 이름 클릭
- `/account` 페이지로 이동

### 2. 직접 URL 접근
- 브라우저 주소창에 `http://localhost:3000/account` 입력
- 다른 페이지에서 링크 클릭

---

## 🔐 인증 Flow

### Server-Side 인증 (page.tsx)

```typescript
// app/(main)/account/page.tsx
export default async function AccountPage() {
  const session = await getServerSession(authOptions);
  
  if (!session?.user) {
    redirect('/login?callbackUrl=/account');
  }
  
  return <AccountInfo />;
}
```

**동작:**
1. 서버에서 `getServerSession()` 호출
2. 세션이 없으면 `/login?callbackUrl=/account`로 리다이렉트
3. 세션이 있으면 `AccountInfo` 컴포넌트 렌더링

### Client-Side 인증 (AccountInfo.tsx)

```typescript
// components/account/AccountInfo.tsx
const { data: session, status } = useSession();

useEffect(() => {
  if (status === 'authenticated') {
    fetchAccountInfo();
  }
}, [status]);
```

**동작:**
1. `useSession()` 훅으로 클라이언트 세션 상태 확인
2. `status === 'authenticated'`일 때만 API 호출
3. 로딩 중이면 스피너 표시

---

## 📊 데이터 로딩 Flow

### 1. API 엔드포인트: `/api/users/me`

```typescript
// app/api/users/me/route.ts
export async function GET(request: NextRequest) {
  const session = await getServerSession(authOptions);
  
  if (!session?.user) {
    return NextResponse.json({ error: '인증이 필요합니다.' }, { status: 401 });
  }
  
  const userId = (session.user as any).id;
  
  const user = await prisma.user.findUnique({
    where: { id: userId },
    include: {
      accounts: { ... },
      sessions: { ... },
      _count: { ... }
    }
  });
  
  return NextResponse.json({ user, accounts, sessions, stats });
}
```

### 2. 데이터 조회 내용

**User 정보:**
- `id`, `email`, `name`, `avatar`
- `provider`, `providerId` (주 로그인 Provider)
- `role` (user/admin)
- `createdAt`, `updatedAt`

**Accounts (OAuth 연결):**
- `id`, `provider` (google/kakao)
- `providerAccountId`
- `type` (oauth)

**Sessions (최근 5개):**
- `id`, `sessionToken`
- `expires`
- `isActive` (만료 여부 계산)

**Stats (통계):**
- `reviews` (작성한 리뷰 수)
- `favorites` (즐겨찾기 수)

### 3. 클라이언트에서 데이터 처리

```typescript
const fetchAccountInfo = async () => {
  try {
    setLoading(true);
    const response = await fetch('/api/users/me');
    
    if (!response.ok) {
      throw new Error('계정 정보를 불러올 수 없습니다.');
    }
    
    const data = await response.json();
    setAccountData(data);
  } catch (error) {
    toast.error('계정 정보를 불러오는 중 오류가 발생했습니다.');
  } finally {
    setLoading(false);
  }
};
```

---

## 🎨 UI 렌더링 Flow

### 로딩 상태
```tsx
if (status === 'loading' || loading) {
  return (
    <div className="flex justify-center items-center py-12">
      <div className="w-8 h-8 border-2 border-primary border-t-transparent rounded-full animate-spin" />
    </div>
  );
}
```

### 에러 상태
```tsx
if (!accountData) {
  return (
    <Card>
      <CardContent className="p-6">
        <p className="text-gray-600">계정 정보를 불러올 수 없습니다.</p>
      </CardContent>
    </Card>
  );
}
```

### 성공 상태: 5개 카드 섹션

#### 1. 기본 정보 카드
- 프로필 이미지/아바타
- 이름, 이메일
- 역할 (사용자/관리자)
- 가입일

#### 2. 연결된 계정 카드
- Google OAuth 계정 (🔵 Google)
- Kakao OAuth 계정 (🟡 Kakao)
- Provider Account ID
- 연결 상태 배지
- 주 로그인 Provider 정보 (파란색 박스)

#### 3. 활동 통계 카드
- 작성한 리뷰 수
- 즐겨찾기 수
- 그리드 레이아웃 (2열)

#### 4. 활성 세션 카드
- 최근 세션 정보 (최대 5개)
- 세션 ID (일부 표시)
- 세션 만료일
- 활성/만료 상태 배지

#### 5. 로그아웃 버튼
- 우측 정렬
- 아이콘 포함
- Toast 알림

---

## 🚪 로그아웃 Flow

```typescript
const handleSignOut = async () => {
  try {
    await signOut({ callbackUrl: '/' });
    toast.success('로그아웃되었습니다.');
  } catch (error) {
    toast.error('로그아웃 중 오류가 발생했습니다.');
  }
};
```

**동작:**
1. `signOut({ callbackUrl: '/' })` 호출
2. NextAuth.js가 세션 삭제
3. 홈페이지(`/`)로 리다이렉트
4. Toast 알림 표시

---

## 🔄 상태 관리

### 상태 변수
```typescript
const { data: session, status } = useSession(); // NextAuth 세션
const [accountData, setAccountData] = useState<AccountData | null>(null);
const [loading, setLoading] = useState(true);
```

### 상태 전이
```
초기 상태
  ↓
status: 'loading' → 스피너 표시
  ↓
status: 'authenticated' → fetchAccountInfo() 호출
  ↓
loading: true → 스피너 표시
  ↓
API 응답 성공 → accountData 설정, loading: false
  ↓
UI 렌더링 (5개 카드)
```

---

## 🛡️ 에러 처리

### 1. 인증 실패
- **Server-side**: `/login?callbackUrl=/account`로 리다이렉트
- **API**: `401 Unauthorized` 응답

### 2. API 호출 실패
- Toast 에러 메시지 표시
- 에러 상태 UI 표시

### 3. 데이터 없음
- "계정 정보를 불러올 수 없습니다." 메시지 표시

---

## 📱 반응형 디자인

### 모바일
- 1열 그리드 레이아웃
- 프로필 이미지와 이름 세로 배치
- Header에서 이름 숨김 (아이콘만 표시)

### 데스크톱
- 2열 그리드 레이아웃 (활동 통계)
- 프로필 이미지와 이름 가로 배치
- Header에서 이름 표시

---

## 🔗 관련 파일

### 페이지
- `app/(main)/account/page.tsx` - 서버 컴포넌트, 인증 확인

### 컴포넌트
- `components/account/AccountInfo.tsx` - 클라이언트 컴포넌트, UI 렌더링
- `components/layout/Header.tsx` - 접근 링크

### API
- `app/api/users/me/route.ts` - 사용자 데이터 조회

### 인증
- `lib/auth.ts` - NextAuth 설정
- `app/api/auth/[...nextauth]/route.ts` - NextAuth API 라우트

---

## ✅ 체크리스트

- [x] 서버 사이드 인증 확인
- [x] 비로그인 시 리다이렉트
- [x] 클라이언트 사이드 세션 확인
- [x] API 호출 및 에러 처리
- [x] 로딩 상태 표시
- [x] 5개 카드 섹션 렌더링
- [x] 로그아웃 기능
- [x] Toast 알림
- [x] 반응형 디자인
- [x] 타입 안정성

---

## 🎯 사용자 시나리오

### 시나리오 1: 로그인한 사용자가 계정 페이지 접근
1. Header에서 프로필 클릭
2. `/account` 페이지 로드
3. 서버에서 세션 확인 (✅ 통과)
4. `AccountInfo` 컴포넌트 렌더링
5. API 호출 (`/api/users/me`)
6. 데이터 표시 (5개 카드)
7. 사용자가 정보 확인

### 시나리오 2: 비로그인 사용자가 계정 페이지 접근
1. 직접 URL 입력 또는 링크 클릭
2. `/account` 페이지 로드
3. 서버에서 세션 확인 (❌ 실패)
4. `/login?callbackUrl=/account`로 리다이렉트
5. 로그인 페이지 표시
6. 로그인 성공 후 `/account`로 자동 이동

### 시나리오 3: 로그아웃
1. 계정 페이지에서 "로그아웃" 버튼 클릭
2. `signOut()` 호출
3. 세션 삭제
4. 홈페이지(`/`)로 리다이렉트
5. Toast 알림 표시

---

## 📝 개선 가능한 사항

1. **새로고침 기능**: 데이터 갱신 버튼 추가
2. **세션 관리**: 특정 세션 종료 기능
3. **계정 연결**: 추가 OAuth 계정 연결 기능
4. **프로필 편집**: 이름, 아바타 수정 기능
5. **활동 내역**: 리뷰/즐겨찾기 상세 목록 링크

